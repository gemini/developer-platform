.nh
.TH "GEMINI-MARKETS" "1" "Apr 2026" "Auto generated by spf13/cobra" ""

.SH NAME
.PP
gemini-markets-completion - Generate shell completion scripts


.SH SYNOPSIS
.PP
\fBgemini-markets completion [bash|zsh|fish|powershell]\fP


.SH DESCRIPTION
.PP
Generate shell completion scripts for gemini-markets.

.PP
To load completions:

.PP
Bash:
  $ source <(gemini-markets completion bash)
  # To load completions for each session, execute once:
  # Linux:
  $ gemini-markets completion bash > /etc/bash_completion.d/gemini-markets
  # macOS:
  $ gemini-markets completion bash > $(brew --prefix)/etc/bash_completion.d/gemini-markets

.PP
Zsh:
  # If shell completion is not already enabled in your environment,
  # you will need to enable it. You can execute the following once:
  $ echo "autoload -U compinit; compinit" >> ~/.zshrc

.PP
# To load completions for each session, execute once:
  $ gemini-markets completion zsh > "${fpath[1]}/_gemini-markets"

.PP
Fish:
  $ gemini-markets completion fish | source
  # To load completions for each session, execute once:
  $ gemini-markets completion fish > ~/.config/fish/completions/gemini-markets.fish

.PP
PowerShell:
  PS> gemini-markets completion powershell | Out-String | Invoke-Expression


.SH OPTIONS
.PP
\fB-h\fP, \fB--help\fP[=false]
	help for completion


.SH OPTIONS INHERITED FROM PARENT COMMANDS
.PP
\fB--debug\fP[=false]
	enable debug logging

.PP
\fB--no-websocket\fP[=false]
	disable WebSocket and use REST API only

.PP
\fB-o\fP, \fB--output\fP="json"
	output format: json, table, or csv

.PP
\fB-q\fP, \fB--quiet\fP[=false]
	suppress stderr output for pure JSON piping

.PP
\fB--raw\fP[=false]
	output compact JSON without pretty-printing

.PP
\fB--sandbox\fP[=false]
	use sandbox environment

.PP
\fB--timeout\fP=30
	request timeout in seconds

.PP
\fB--verbose\fP[=false]
	use full field names in JSON output (default: abbreviated)


.SH SEE ALSO
.PP
\fBgemini-markets(1)\fP


.SH HISTORY
.PP
25-Apr-2026 Auto generated by spf13/cobra
